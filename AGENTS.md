# Conventions — arpeiks/skillz

> Engineering conventions for all projects. Source of truth:
> https://github.com/arpeiks/skillz — do not edit this file in a project;
> re-sync it instead.

Version: 0.2.0 · Updated: 2026-09-06

## Always apply

These are non-negotiable and apply to every task, with no need to fetch
anything further.

### Git

- Commit **only what is already staged**. Never `git add -A`, `git add .`, or
  `git commit -a` — unstaged changes are unstaged on purpose.
- Keep commit messages short.
- Never add a co-author trailer.
- Never rewrite pushed history — no `reset`, `amend`, or force push. Fix forward.

### Code

- Use **Bun**. New frontend projects: TanStack Start + Router + Query, Tailwind 4.
- File names are lowercase **kebab-case**.
- Components are **PascalCase, prefixed with their container** — `RouterShell`,
  `RouterNotFound`.
- Prefer `export function Name() {}` over `export const Name = () => {}`.
  Default exports only where a library demands one.
- Import internals via the `#/*` subpath alias, never deep relative paths.
- **Never format by hand.** Run the project's `format` script.
- Imports are sorted **by line length, shortest first**, as one block with no
  blank lines between them. Side-effect imports keep their position.

## Full detail

Fetch these when the task needs more than the rules above.

- [Frontend](https://raw.githubusercontent.com/arpeiks/skillz/main/plugins/conventions/skills/frontend-conventions/SKILL.md): stack, folder structure, route
  files, formatting toolchain, pyramid import ordering, config files.
- [General](https://raw.githubusercontent.com/arpeiks/skillz/main/plugins/conventions/skills/general-conventions/SKILL.md): git and commit discipline in full.
